{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"qualdatan-plugins","text":"<p>Plugin-Manager f\u00fcr Qualdatan: findet, installiert und verifiziert Bundles (YAML-Daten-Pakete mit Facets/Codebooks/Methoden/Layouts). Tap-Style \u2014 Bundles leben in GitHub-Repos der Publisher, ein sp\u00e4terer Plugin-Server listet sie auf.</p> <p>Status: Scaffold. Phase C f\u00fcllt <code>manager</code>, <code>registry</code>, <code>loader</code>, <code>verify</code>, <code>cache</code>, <code>server_client</code>, <code>bundle</code> mit Leben.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install qualdatan-plugins\n</code></pre>"},{"location":"#weiter","title":"Weiter","text":"<ul> <li>Architecture \u2014 Tap-Modell, Verantwortlichkeiten der Subpakete.</li> <li>API Reference \u2014 wird aus Docstrings erzeugt (noch leer, Modul-Phase C).</li> <li>Changelog.</li> </ul>"},{"location":"#lizenz","title":"Lizenz","text":"<p>AGPL-3.0-only \u2014 siehe LICENSE.</p>"},{"location":"api/","title":"API Reference","text":"<p>Wird aus den Docstrings generiert. Aktuell nur das Paket-Root \u2014 weitere Module werden hier als <code>:::</code>-Direktive eingetragen, sobald sie in Phase C existieren.</p>"},{"location":"api/#paket","title":"Paket","text":""},{"location":"api/#qualdatan_plugins","title":"qualdatan_plugins","text":"<p>Qualdatan plugin manager.</p> <p>Verwaltet Community-Bundles \u2014 YAML-Daten-Pakete mit Facets, Codebooks, Methoden und Ordner-Layouts fuer konkrete Analyse-Domaenen.</p>"},{"location":"api/#qualdatan_plugins--module","title":"Module","text":"<ul> <li>:mod:<code>.bundle</code>         Manifest-Datentypen + YAML-Loader</li> <li>:mod:<code>.cache</code>          Filesystem-Cache fuer installierte Bundles</li> <li>:mod:<code>.registry</code>       SQLite-Index + per-Projekt-Aktivierung</li> <li>:mod:<code>.loader</code>         :class:<code>BundleSource</code> (erfuellt   :class:<code>qualdatan_core.PluginSource</code>)</li> <li>:mod:<code>.verify</code>         Manifest- und Integritaets-Checks</li> <li>:mod:<code>.server_client</code>  HTTP-Client fuer den Tap-Style Plugin-Server</li> <li>:mod:<code>.manager</code>        :class:<code>PluginManager</code> \u2014 High-Level-Orchestrator</li> </ul> <p>Vgl. den Gesamtplan: https://github.com/GeneralPawz/Qualdatan</p>"},{"location":"api/#qualdatan_plugins.BundleError","title":"BundleError","text":"<p>               Bases: <code>ValueError</code></p> <p>Fehler beim Laden / Validieren eines Bundles.</p>"},{"location":"api/#qualdatan_plugins.BundleManifest","title":"BundleManifest  <code>dataclass</code>","text":"<pre><code>BundleManifest(id, version, label, description, author, homepage, license, requires, facets, codebooks, methods, layouts, root)\n</code></pre> <p>Parsed <code>bundle.yaml</code>.</p> <p>Pfade in <code>facets</code>/<code>codebooks</code>/<code>methods</code>/<code>layouts</code> sind relativ zu <code>root</code>; Konsumenten koennen <code>manifest.resolve(p)</code> benutzen.</p>"},{"location":"api/#qualdatan_plugins.BundleManifest.resolve","title":"resolve","text":"<pre><code>resolve(rel)\n</code></pre> <p>Absoluter Pfad zu einer im Manifest genannten Datei.</p> Source code in <code>src/qualdatan_plugins/bundle.py</code> <pre><code>def resolve(self, rel: str) -&gt; Path:\n    \"\"\"Absoluter Pfad zu einer im Manifest genannten Datei.\"\"\"\n    return (self.root / rel).resolve()\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleManifest.ref","title":"ref","text":"<pre><code>ref()\n</code></pre> <p>Bundle-Referenz <code>namespace/name@version</code>.</p> Source code in <code>src/qualdatan_plugins/bundle.py</code> <pre><code>def ref(self) -&gt; str:\n    \"\"\"Bundle-Referenz ``namespace/name@version``.\"\"\"\n    return f\"{self.id}@{self.version}\"\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleRequirement","title":"BundleRequirement  <code>dataclass</code>","text":"<pre><code>BundleRequirement(package, version_spec)\n</code></pre> <p>Eine Paketabhaengigkeit im Bundle-Manifest (<code>requires</code>-Sektion).</p>"},{"location":"api/#qualdatan_plugins.InstalledBundle","title":"InstalledBundle  <code>dataclass</code>","text":"<pre><code>InstalledBundle(manifest, install_path, source, origin='', commit_sha='', enabled=True)\n</code></pre> <p>Ein installiertes Bundle im lokalen Cache.</p> <p><code>source</code> beschreibt, woher das Bundle kam (<code>\"local\"</code>, <code>\"git\"</code>, <code>\"zip\"</code>). <code>commit_sha</code> ist bei Git-Installs gesetzt und erlaubt es dem Registry-Server, Integritaet gegen den GitHub-Commit zu pruefen.</p>"},{"location":"api/#qualdatan_plugins.BundleCache","title":"BundleCache","text":"<pre><code>BundleCache(root=None)\n</code></pre> <p>Verwaltet Bundle-Dateien im lokalen Cache-Verzeichnis.</p> <p>Die Klasse kapselt Kopier-, Git-Clone- und Remove-Operationen und kennt keine Registry-Datenbank; Aktivierungs-State lebt in :class:<code>~qualdatan_plugins.registry.PluginRegistry</code>.</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def __init__(self, root: Path | None = None) -&gt; None:\n    self._root = Path(root) if root is not None else _default_cache_root()\n    self._root.mkdir(parents=True, exist_ok=True)\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleCache.root","title":"root  <code>property</code>","text":"<pre><code>root\n</code></pre> <p>Wurzelverzeichnis des Caches.</p>"},{"location":"api/#qualdatan_plugins.BundleCache.path_for","title":"path_for","text":"<pre><code>path_for(bundle_id, version)\n</code></pre> <p>Zielpfad fuer ein konkretes Bundle (kann noch nicht existieren).</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def path_for(self, bundle_id: str, version: str) -&gt; Path:\n    \"\"\"Zielpfad fuer ein konkretes Bundle (kann noch nicht existieren).\"\"\"\n    ns, name = _split_id(bundle_id)\n    return self._root / ns / name / version\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleCache.exists","title":"exists","text":"<pre><code>exists(bundle_id, version)\n</code></pre> <p>True, wenn das Bundle bereits im Cache liegt.</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def exists(self, bundle_id: str, version: str) -&gt; bool:\n    \"\"\"True, wenn das Bundle bereits im Cache liegt.\"\"\"\n    target = self.path_for(bundle_id, version)\n    return target.is_dir() and (target / \"bundle.yaml\").is_file()\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleCache.install_from_dir","title":"install_from_dir","text":"<pre><code>install_from_dir(src, *, expected_id=None)\n</code></pre> <p>Kopiert ein Bundle aus einem lokalen Verzeichnis in den Cache.</p> <p>Parameters:</p> Name Type Description Default <code>src</code> <code>Path</code> <p>Verzeichnis mit <code>bundle.yaml</code> am Root.</p> required <code>expected_id</code> <code>str | None</code> <p>wenn gesetzt, muss die Manifest-Id exakt matchen.</p> <code>None</code> <p>Returns:</p> Type Description <code>InstalledBundle</code> <p>class:<code>InstalledBundle</code> mit <code>source=\"local\"</code>.</p> <p>Raises:</p> Type Description <code>BundleError</code> <p>Manifest fehlt/invalide oder <code>expected_id</code> mismatch.</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def install_from_dir(\n    self,\n    src: Path,\n    *,\n    expected_id: str | None = None,\n) -&gt; InstalledBundle:\n    \"\"\"Kopiert ein Bundle aus einem lokalen Verzeichnis in den Cache.\n\n    Args:\n        src: Verzeichnis mit ``bundle.yaml`` am Root.\n        expected_id: wenn gesetzt, muss die Manifest-Id exakt matchen.\n\n    Returns:\n        :class:`InstalledBundle` mit ``source=\"local\"``.\n\n    Raises:\n        BundleError: Manifest fehlt/invalide oder ``expected_id`` mismatch.\n    \"\"\"\n    src = Path(src).resolve()\n    manifest = load_manifest(src)\n    if expected_id is not None and manifest.id != expected_id:\n        raise BundleError(\n            f\"bundle-id mismatch: manifest={manifest.id}, erwartet={expected_id}\"\n        )\n    target = self.path_for(manifest.id, manifest.version)\n    target.parent.mkdir(parents=True, exist_ok=True)\n\n    # atomarer Swap: erst in tmp-Nachbarverzeichnis kopieren, dann replace\n    tmp_parent = target.parent\n    tmp_dir = Path(tempfile.mkdtemp(prefix=f\".tmp-{manifest.name}-\", dir=tmp_parent))\n    staging = tmp_dir / \"stage\"\n    try:\n        shutil.copytree(src, staging)\n        if target.exists():\n            # existierenden Install entfernen (Re-Install / Overwrite)\n            shutil.rmtree(target)\n        os.replace(staging, target)\n    except Exception:\n        # cleanup auf Fehler\n        if staging.exists():\n            shutil.rmtree(staging, ignore_errors=True)\n        raise\n    finally:\n        # tmp_dir nur entfernen, wenn leer (staging wurde wegverschoben)\n        try:\n            tmp_dir.rmdir()\n        except OSError:\n            shutil.rmtree(tmp_dir, ignore_errors=True)\n\n    installed_manifest = load_manifest(target)\n    return InstalledBundle(\n        manifest=installed_manifest,\n        install_path=target,\n        source=\"local\",\n        origin=str(src),\n    )\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleCache.install_from_git","title":"install_from_git","text":"<pre><code>install_from_git(repo_url, version, *, commit_sha='')\n</code></pre> <p>Klont ein Git-Repo (Tag <code>v&lt;version&gt;</code>) und installiert daraus.</p> <p>Parameters:</p> Name Type Description Default <code>repo_url</code> <code>str</code> <p>Git-URL (z.B. <code>https://github.com/org/bundle.git</code>).</p> required <code>version</code> <code>str</code> <p>Semver ohne <code>v</code>-Prefix; der Tag heisst <code>v&lt;version&gt;</code>.</p> required <code>commit_sha</code> <code>str</code> <p>optional; wird in :class:<code>InstalledBundle</code> gespeichert, falls der Aufrufer einen bereits verifizierten SHA hat. Wird nicht angegeben, liest die Methode <code>git rev-parse HEAD</code>.</p> <code>''</code> <p>Returns:</p> Type Description <code>InstalledBundle</code> <p>class:<code>InstalledBundle</code> mit <code>source=\"git\"</code>.</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def install_from_git(\n    self,\n    repo_url: str,\n    version: str,\n    *,\n    commit_sha: str = \"\",\n) -&gt; InstalledBundle:\n    \"\"\"Klont ein Git-Repo (Tag ``v&lt;version&gt;``) und installiert daraus.\n\n    Args:\n        repo_url: Git-URL (z.B. ``https://github.com/org/bundle.git``).\n        version: Semver ohne ``v``-Prefix; der Tag heisst ``v&lt;version&gt;``.\n        commit_sha: optional; wird in :class:`InstalledBundle` gespeichert,\n            falls der Aufrufer einen bereits verifizierten SHA hat. Wird\n            nicht angegeben, liest die Methode ``git rev-parse HEAD``.\n\n    Returns:\n        :class:`InstalledBundle` mit ``source=\"git\"``.\n    \"\"\"\n    with tempfile.TemporaryDirectory(prefix=\"qualdatan-git-\") as tmp:\n        tmp_path = Path(tmp) / \"clone\"\n        subprocess.run(\n            [\n                \"git\",\n                \"clone\",\n                \"--depth=1\",\n                \"--branch\",\n                f\"v{version}\",\n                repo_url,\n                str(tmp_path),\n            ],\n            check=True,\n            capture_output=True,\n        )\n        if not commit_sha:\n            result = subprocess.run(\n                [\"git\", \"-C\", str(tmp_path), \"rev-parse\", \"HEAD\"],\n                check=True,\n                capture_output=True,\n                text=True,\n            )\n            commit_sha = result.stdout.strip()\n\n        local = self.install_from_dir(tmp_path)\n\n    return InstalledBundle(\n        manifest=local.manifest,\n        install_path=local.install_path,\n        source=\"git\",\n        origin=repo_url,\n        commit_sha=commit_sha,\n    )\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleCache.remove","title":"remove","text":"<pre><code>remove(bundle_id, version)\n</code></pre> <p>Loescht ein Bundle aus dem Cache (no-op wenn nicht vorhanden).</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def remove(self, bundle_id: str, version: str) -&gt; None:\n    \"\"\"Loescht ein Bundle aus dem Cache (no-op wenn nicht vorhanden).\"\"\"\n    target = self.path_for(bundle_id, version)\n    if target.exists():\n        shutil.rmtree(target)\n    # leere Parent-Verzeichnisse aufraeumen\n    parent = target.parent\n    for _ in range(2):  # name/, namespace/\n        try:\n            parent.rmdir()\n        except OSError:\n            break\n        parent = parent.parent\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleCache.iter_installed","title":"iter_installed","text":"<pre><code>iter_installed()\n</code></pre> <p>Walked den Cache und liefert alle valide geparsten Bundles.</p> Source code in <code>src/qualdatan_plugins/cache.py</code> <pre><code>def iter_installed(self) -&gt; Iterable[InstalledBundle]:\n    \"\"\"Walked den Cache und liefert alle valide geparsten Bundles.\"\"\"\n    if not self._root.is_dir():\n        return\n    for ns_dir in sorted(self._root.iterdir()):\n        if not ns_dir.is_dir():\n            continue\n        for name_dir in sorted(ns_dir.iterdir()):\n            if not name_dir.is_dir():\n                continue\n            for version_dir in sorted(name_dir.iterdir()):\n                if not version_dir.is_dir():\n                    continue\n                if not (version_dir / \"bundle.yaml\").is_file():\n                    continue\n                try:\n                    manifest = load_manifest(version_dir)\n                except BundleError:\n                    continue\n                yield InstalledBundle(\n                    manifest=manifest,\n                    install_path=version_dir,\n                    source=\"local\",\n                    origin=str(version_dir),\n                )\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleSource","title":"BundleSource","text":"<pre><code>BundleSource(bundles)\n</code></pre> <p>PluginSource-Adapter ueber eine Sequenz installierter Bundles.</p> <p>Nur aktivierte Bundles (<code>enabled=True</code>) werden in den Iterator- Methoden beruecksichtigt; deaktivierte Bundles bleiben aber in der :attr:<code>bundles</code>-Tuple erhalten, damit der Manager sie auflisten kann.</p> Example <p>src = BundleSource([ib1, ib2]) list(src.iter_facets()) [...]</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def __init__(self, bundles: Iterable[InstalledBundle]) -&gt; None:\n    self._bundles: tuple[InstalledBundle, ...] = tuple(bundles)\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleSource.bundles","title":"bundles  <code>property</code>","text":"<pre><code>bundles\n</code></pre> <p>Alle Bundles (auch deaktivierte), in Konstruktor-Reihenfolge.</p>"},{"location":"api/#qualdatan_plugins.BundleSource.iter_facets","title":"iter_facets","text":"<pre><code>iter_facets()\n</code></pre> <p>Alle Facets aller aktiven Bundles.</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def iter_facets(self) -&gt; Iterable[Facet]:\n    \"\"\"Alle Facets aller aktiven Bundles.\"\"\"\n\n    for ib in self._active():\n        yield from load_bundle_facets(ib)\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleSource.iter_codebook_paths","title":"iter_codebook_paths","text":"<pre><code>iter_codebook_paths()\n</code></pre> <p>Absolute Pfade zu allen Codebooks aller aktiven Bundles.</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def iter_codebook_paths(self) -&gt; Iterable[Path]:\n    \"\"\"Absolute Pfade zu allen Codebooks aller aktiven Bundles.\"\"\"\n\n    for ib in self._active():\n        for rel in ib.manifest.codebooks:\n            yield ib.manifest.resolve(rel)\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleSource.iter_method_paths","title":"iter_method_paths","text":"<pre><code>iter_method_paths()\n</code></pre> <p>Absolute Pfade zu allen Methods aller aktiven Bundles.</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def iter_method_paths(self) -&gt; Iterable[Path]:\n    \"\"\"Absolute Pfade zu allen Methods aller aktiven Bundles.\"\"\"\n\n    for ib in self._active():\n        for rel in ib.manifest.methods:\n            yield ib.manifest.resolve(rel)\n</code></pre>"},{"location":"api/#qualdatan_plugins.BundleSource.iter_layouts","title":"iter_layouts","text":"<pre><code>iter_layouts()\n</code></pre> <p>Alle Layouts aller aktiven Bundles.</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def iter_layouts(self) -&gt; Iterable[FolderLayout]:\n    \"\"\"Alle Layouts aller aktiven Bundles.\"\"\"\n\n    for ib in self._active():\n        yield from load_bundle_layouts(ib)\n</code></pre>"},{"location":"api/#qualdatan_plugins.InstallResult","title":"InstallResult  <code>dataclass</code>","text":"<pre><code>InstallResult(bundle, verification)\n</code></pre> <p>Ergebnis einer Install-Operation.</p>"},{"location":"api/#qualdatan_plugins.PluginManager","title":"PluginManager","text":"<pre><code>PluginManager(cache=None, registry=None, *, verify_on_install=True)\n</code></pre> <p>Koordiniert Cache, Registry, Loader und Verify.</p> <p>Parameters:</p> Name Type Description Default <code>cache</code> <code>BundleCache | None</code> <p>optionaler :class:<code>BundleCache</code>; Default nutzt Env/platformdirs.</p> <code>None</code> <code>registry</code> <code>PluginRegistry | None</code> <p>optionale :class:<code>PluginRegistry</code>; Default nutzt Env/platformdirs.</p> <code>None</code> <code>verify_on_install</code> <code>bool</code> <p>wenn <code>True</code> (Default), schlaegt install fehl, falls das Manifest Integritaetsprobleme hat.</p> <code>True</code> Source code in <code>src/qualdatan_plugins/manager.py</code> <pre><code>def __init__(\n    self,\n    cache: BundleCache | None = None,\n    registry: PluginRegistry | None = None,\n    *,\n    verify_on_install: bool = True,\n) -&gt; None:\n    self._cache = cache or BundleCache()\n    self._registry = registry or PluginRegistry()\n    self._verify_on_install = verify_on_install\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginManager.install_from_path","title":"install_from_path","text":"<pre><code>install_from_path(src, *, expected_id=None)\n</code></pre> <p>Installiert ein lokales Bundle-Verzeichnis.</p> <p>Parameters:</p> Name Type Description Default <code>src</code> <code>Path</code> <p>Ordner mit <code>bundle.yaml</code> im Wurzelverzeichnis.</p> required <code>expected_id</code> <code>str | None</code> <p>optional; wenn gesetzt, muss die Manifest-Id matchen.</p> <code>None</code> <p>Returns:</p> Type Description <code>InstallResult</code> <p>class:<code>InstallResult</code> mit dem registrierten :class:<code>InstalledBundle</code></p> <code>InstallResult</code> <p>und dem Verifikations-Report.</p> <p>Raises:</p> Type Description <code>PluginManagerError</code> <p>wenn Verifikation mit <code>verify_on_install=True</code> fehlschlaegt.</p> Source code in <code>src/qualdatan_plugins/manager.py</code> <pre><code>def install_from_path(\n    self, src: Path, *, expected_id: str | None = None\n) -&gt; InstallResult:\n    \"\"\"Installiert ein lokales Bundle-Verzeichnis.\n\n    Args:\n        src: Ordner mit ``bundle.yaml`` im Wurzelverzeichnis.\n        expected_id: optional; wenn gesetzt, muss die Manifest-Id matchen.\n\n    Returns:\n        :class:`InstallResult` mit dem registrierten :class:`InstalledBundle`\n        und dem Verifikations-Report.\n\n    Raises:\n        PluginManagerError: wenn Verifikation mit ``verify_on_install=True``\n            fehlschlaegt.\n    \"\"\"\n\n    ib = self._cache.install_from_dir(src, expected_id=expected_id)\n    return self._finalize_install(ib)\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginManager.install_from_git","title":"install_from_git","text":"<pre><code>install_from_git(repo_url, version, *, commit_sha='')\n</code></pre> <p>Installiert ein Bundle via <code>git clone --depth=1 --branch=v&lt;version&gt;</code>.</p> Source code in <code>src/qualdatan_plugins/manager.py</code> <pre><code>def install_from_git(\n    self, repo_url: str, version: str, *, commit_sha: str = \"\"\n) -&gt; InstallResult:\n    \"\"\"Installiert ein Bundle via ``git clone --depth=1 --branch=v&lt;version&gt;``.\"\"\"\n\n    ib = self._cache.install_from_git(repo_url, version, commit_sha=commit_sha)\n    return self._finalize_install(ib)\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginManager.uninstall","title":"uninstall","text":"<pre><code>uninstall(bundle_id, version=None)\n</code></pre> <p>Entfernt ein installiertes Bundle (Cache + Registry).</p> <p>Ohne <code>version</code> wird die aktuell registrierte Version entfernt.</p> Source code in <code>src/qualdatan_plugins/manager.py</code> <pre><code>def uninstall(self, bundle_id: str, version: str | None = None) -&gt; None:\n    \"\"\"Entfernt ein installiertes Bundle (Cache + Registry).\n\n    Ohne ``version`` wird die aktuell registrierte Version entfernt.\n    \"\"\"\n\n    ib = self._registry.get_installed(bundle_id, version)\n    if ib is None:\n        raise PluginManagerError(f\"Bundle '{bundle_id}' ist nicht installiert\")\n    self._registry.remove_install(ib.id, ib.version)\n    try:\n        self._cache.remove(ib.id, ib.version)\n    except BundleError:\n        # Cache-Eintrag war evtl. schon weg \u2014 kein harter Fehler.\n        pass\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginManager.source_for","title":"source_for","text":"<pre><code>source_for(project_id='')\n</code></pre> <p>Baut einen :class:<code>BundleSource</code> aus den aktiven Bundles.</p> <p>Wenn fuer ein Projekt nichts aktiv ist, faellt die Methode auf den globalen Default (<code>project_id=\"\"</code>) zurueck.</p> Source code in <code>src/qualdatan_plugins/manager.py</code> <pre><code>def source_for(self, project_id: str = \"\") -&gt; BundleSource:\n    \"\"\"Baut einen :class:`BundleSource` aus den aktiven Bundles.\n\n    Wenn fuer ein Projekt nichts aktiv ist, faellt die Methode auf den\n    globalen Default (``project_id=\"\"``) zurueck.\n    \"\"\"\n\n    active = self._registry.list_active(project_id)\n    if not active and project_id:\n        active = self._registry.list_active(\"\")\n    return BundleSource(active)\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginManager.discover_local","title":"discover_local","text":"<pre><code>discover_local(root)\n</code></pre> <p>Findet Bundle-Wurzeln unter <code>root</code> (rekursiv, 2 Ebenen).</p> <p>Ein Ordner gilt als Bundle, wenn er eine <code>bundle.yaml</code> enthaelt.</p> Source code in <code>src/qualdatan_plugins/manager.py</code> <pre><code>def discover_local(self, root: Path) -&gt; list[Path]:\n    \"\"\"Findet Bundle-Wurzeln unter ``root`` (rekursiv, 2 Ebenen).\n\n    Ein Ordner gilt als Bundle, wenn er eine ``bundle.yaml`` enthaelt.\n    \"\"\"\n\n    root = Path(root)\n    if not root.exists():\n        return []\n    found: list[Path] = []\n    if (root / \"bundle.yaml\").is_file():\n        found.append(root)\n    for child in sorted(root.iterdir()) if root.is_dir() else ():\n        if child.is_dir() and (child / \"bundle.yaml\").is_file():\n            found.append(child)\n    return found\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginManagerError","title":"PluginManagerError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Fehler auf Manager-Ebene (Install/Activate/Verify).</p>"},{"location":"api/#qualdatan_plugins.PluginRegistry","title":"PluginRegistry","text":"<pre><code>PluginRegistry(db_path=None)\n</code></pre> <p>SQLite-Index ueber alle installierten Bundles.</p> Persistenz <p>Der DB-Pfad kommt aus <code>QUALDATAN_PLUGIN_REGISTRY</code> oder default <code>&lt;platformdirs appdata&gt;/plugins.db</code>. Bei Fehlen wird Schema automatisch angelegt.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def __init__(self, db_path: Path | None = None) -&gt; None:\n    self._db_path = Path(db_path) if db_path is not None else _default_db_path()\n    self._db_path.parent.mkdir(parents=True, exist_ok=True)\n    self._conn: sqlite3.Connection = sqlite3.connect(str(self._db_path))\n    self._conn.execute(\"PRAGMA foreign_keys = ON\")\n    self._ensure_schema()\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.db_path","title":"db_path  <code>property</code>","text":"<pre><code>db_path\n</code></pre> <p>Pfad zur SQLite-Datei.</p>"},{"location":"api/#qualdatan_plugins.PluginRegistry.record_install","title":"record_install","text":"<pre><code>record_install(ib)\n</code></pre> <p>Legt oder aktualisiert einen Eintrag in der <code>installed</code>-Tabelle.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def record_install(self, ib: InstalledBundle) -&gt; None:\n    \"\"\"Legt oder aktualisiert einen Eintrag in der ``installed``-Tabelle.\"\"\"\n    now = datetime.now(timezone.utc).isoformat()\n    self._conn.execute(\n        \"\"\"\n        INSERT INTO installed\n            (bundle_id, version, install_path, source, origin, commit_sha, installed_at)\n        VALUES (?, ?, ?, ?, ?, ?, ?)\n        ON CONFLICT(bundle_id, version) DO UPDATE SET\n            install_path=excluded.install_path,\n            source=excluded.source,\n            origin=excluded.origin,\n            commit_sha=excluded.commit_sha,\n            installed_at=excluded.installed_at\n        \"\"\",\n        (\n            ib.id,\n            ib.version,\n            str(ib.install_path),\n            ib.source,\n            ib.origin,\n            ib.commit_sha,\n            now,\n        ),\n    )\n    self._conn.commit()\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.remove_install","title":"remove_install","text":"<pre><code>remove_install(bundle_id, version)\n</code></pre> <p>Loescht installed- und zugehoerige active-Eintraege.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def remove_install(self, bundle_id: str, version: str) -&gt; None:\n    \"\"\"Loescht installed- und zugehoerige active-Eintraege.\"\"\"\n    self._conn.execute(\n        \"DELETE FROM active WHERE bundle_id=? AND version=?\",\n        (bundle_id, version),\n    )\n    self._conn.execute(\n        \"DELETE FROM installed WHERE bundle_id=? AND version=?\",\n        (bundle_id, version),\n    )\n    self._conn.commit()\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.list_installed","title":"list_installed","text":"<pre><code>list_installed()\n</code></pre> <p>Alle DB-Eintraege, Manifest re-parsed von Disk.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def list_installed(self) -&gt; list[InstalledBundle]:\n    \"\"\"Alle DB-Eintraege, Manifest re-parsed von Disk.\"\"\"\n    cur = self._conn.execute(\n        \"SELECT bundle_id, version, install_path, source, origin, commit_sha, installed_at \"\n        \"FROM installed ORDER BY bundle_id, version\"\n    )\n    out: list[InstalledBundle] = []\n    for row in cur.fetchall():\n        ib = self._row_to_installed(row)\n        if ib is not None:\n            out.append(ib)\n    return out\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.get_installed","title":"get_installed","text":"<pre><code>get_installed(bundle_id, version=None)\n</code></pre> <p>Einen installierten Eintrag holen.</p> <p>Parameters:</p> Name Type Description Default <code>bundle_id</code> <code>str</code> <p><code>namespace/name</code>.</p> required <code>version</code> <code>str | None</code> <p>exakter Semver; wenn <code>None</code>, wird der (lexikographisch) neueste Eintrag geliefert.</p> <code>None</code> <p>Returns:</p> Type Description <code>InstalledBundle | None</code> <p>class:<code>InstalledBundle</code> oder <code>None</code>.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def get_installed(\n    self,\n    bundle_id: str,\n    version: str | None = None,\n) -&gt; InstalledBundle | None:\n    \"\"\"Einen installierten Eintrag holen.\n\n    Args:\n        bundle_id: ``namespace/name``.\n        version: exakter Semver; wenn ``None``, wird der (lexikographisch)\n            neueste Eintrag geliefert.\n\n    Returns:\n        :class:`InstalledBundle` oder ``None``.\n    \"\"\"\n    if version is None:\n        cur = self._conn.execute(\n            \"SELECT bundle_id, version, install_path, source, origin, commit_sha, \"\n            \"installed_at FROM installed WHERE bundle_id=? ORDER BY version DESC LIMIT 1\",\n            (bundle_id,),\n        )\n    else:\n        cur = self._conn.execute(\n            \"SELECT bundle_id, version, install_path, source, origin, commit_sha, \"\n            \"installed_at FROM installed WHERE bundle_id=? AND version=?\",\n            (bundle_id, version),\n        )\n    row = cur.fetchone()\n    if row is None:\n        return None\n    return self._row_to_installed(row)\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.activate","title":"activate","text":"<pre><code>activate(bundle_id, version, project_id='')\n</code></pre> <p>Markiert <code>&lt;bundle_id&gt;@&lt;version&gt;</code> als aktiv in <code>project_id</code>.</p> <p>Leerer <code>project_id</code> bedeutet globaler Default.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def activate(\n    self,\n    bundle_id: str,\n    version: str,\n    project_id: str = \"\",\n) -&gt; None:\n    \"\"\"Markiert ``&lt;bundle_id&gt;@&lt;version&gt;`` als aktiv in ``project_id``.\n\n    Leerer ``project_id`` bedeutet globaler Default.\n    \"\"\"\n    cur = self._conn.execute(\n        \"SELECT 1 FROM installed WHERE bundle_id=? AND version=?\",\n        (bundle_id, version),\n    )\n    if cur.fetchone() is None:\n        raise BundleError(\n            f\"cannot activate {bundle_id}@{version}: not in installed table\"\n        )\n    self._conn.execute(\n        \"\"\"\n        INSERT INTO active (project_id, bundle_id, version) VALUES (?, ?, ?)\n        ON CONFLICT(project_id, bundle_id) DO UPDATE SET version=excluded.version\n        \"\"\",\n        (project_id, bundle_id, version),\n    )\n    self._conn.commit()\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.deactivate","title":"deactivate","text":"<pre><code>deactivate(bundle_id, project_id='')\n</code></pre> <p>Entfernt einen aktiven Eintrag (no-op wenn nicht vorhanden).</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def deactivate(self, bundle_id: str, project_id: str = \"\") -&gt; None:\n    \"\"\"Entfernt einen aktiven Eintrag (no-op wenn nicht vorhanden).\"\"\"\n    self._conn.execute(\n        \"DELETE FROM active WHERE project_id=? AND bundle_id=?\",\n        (project_id, bundle_id),\n    )\n    self._conn.commit()\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.list_active","title":"list_active","text":"<pre><code>list_active(project_id='')\n</code></pre> <p>Liste der aktiven Bundles in <code>project_id</code>.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def list_active(self, project_id: str = \"\") -&gt; list[InstalledBundle]:\n    \"\"\"Liste der aktiven Bundles in ``project_id``.\"\"\"\n    cur = self._conn.execute(\n        \"\"\"\n        SELECT i.bundle_id, i.version, i.install_path, i.source, i.origin,\n               i.commit_sha, i.installed_at\n        FROM active a\n        JOIN installed i\n          ON i.bundle_id = a.bundle_id AND i.version = a.version\n        WHERE a.project_id = ?\n        ORDER BY i.bundle_id\n        \"\"\",\n        (project_id,),\n    )\n    out: list[InstalledBundle] = []\n    for row in cur.fetchall():\n        ib = self._row_to_installed(row)\n        if ib is not None:\n            out.append(ib)\n    return out\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginRegistry.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Schliesst die SQLite-Verbindung.</p> Source code in <code>src/qualdatan_plugins/registry.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Schliesst die SQLite-Verbindung.\"\"\"\n    try:\n        self._conn.close()\n    except sqlite3.Error:\n        pass\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerClient","title":"PluginServerClient","text":"<pre><code>PluginServerClient(base_url=None, *, token=None, timeout=10.0, client=None)\n</code></pre> <p>HTTP-Client fuer den Qualdatan-Plugin-Server-Index.</p> <p>Standard-URL ist ein Platzhalter und kann ueberschrieben werden via Konstruktor-Argument oder Umgebungsvariable <code>QUALDATAN_PLUGIN_SERVER</code>.</p> <p>Der Client ist stateless; Authentifizierung erfolgt per Bearer-Token (OAuth, vom Login-Flow ausserhalb dieses Moduls erzeugt).</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str | None</code> <p>Basis-URL des Servers. Prioritaet: ctor-Arg &gt; <code>QUALDATAN_PLUGIN_SERVER</code> &gt; :attr:<code>DEFAULT_URL</code>.</p> <code>None</code> <code>token</code> <code>str | None</code> <p>Optionaler Bearer-Token fuer geschuetzte Endpunkte.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>HTTP-Timeout in Sekunden.</p> <code>10.0</code> <code>client</code> <code>Client | None</code> <p>Optionaler vorkonfigurierter <code>httpx.Client</code>. Wird dieser uebergeben, wird er in :meth:<code>close</code> nicht geschlossen.</p> <code>None</code> Source code in <code>src/qualdatan_plugins/server_client.py</code> <pre><code>def __init__(\n    self,\n    base_url: str | None = None,\n    *,\n    token: str | None = None,\n    timeout: float = 10.0,\n    client: httpx.Client | None = None,\n) -&gt; None:\n    resolved = base_url or os.environ.get(\"QUALDATAN_PLUGIN_SERVER\") or self.DEFAULT_URL\n    self._base_url = resolved.rstrip(\"/\")\n    self._token = token\n    self._timeout = timeout\n    if client is not None:\n        self._client = client\n        self._owns_client = False\n    else:\n        self._client = httpx.Client(timeout=timeout)\n        self._owns_client = True\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerClient.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Schliesse den internen <code>httpx.Client</code> (nur wenn selbst erzeugt).</p> Source code in <code>src/qualdatan_plugins/server_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Schliesse den internen ``httpx.Client`` (nur wenn selbst erzeugt).\"\"\"\n    if self._owns_client:\n        self._client.close()\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerClient.search","title":"search","text":"<pre><code>search(query='', *, limit=25)\n</code></pre> <p>Suche Taps per Freitext.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Suchbegriff (leer = alle).</p> <code>''</code> <code>limit</code> <code>int</code> <p>Maximale Ergebnismenge.</p> <code>25</code> <p>Returns:</p> Type Description <code>list[TapEntry]</code> <p>Liste der Treffer-Eintraege.</p> Source code in <code>src/qualdatan_plugins/server_client.py</code> <pre><code>def search(self, query: str = \"\", *, limit: int = 25) -&gt; list[TapEntry]:\n    \"\"\"Suche Taps per Freitext.\n\n    Args:\n        query: Suchbegriff (leer = alle).\n        limit: Maximale Ergebnismenge.\n\n    Returns:\n        Liste der Treffer-Eintraege.\n    \"\"\"\n    payload = self._request(\"GET\", \"/index/search\", params={\"q\": query, \"limit\": limit})\n    if not isinstance(payload, dict) or \"results\" not in payload:\n        raise PluginServerError(\"search: expected {'results': [...]} payload\")\n    results = payload[\"results\"]\n    if not isinstance(results, list):\n        raise PluginServerError(\"search: 'results' is not a list\")\n    return [_parse_entry(r) for r in results]\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerClient.get_tap","title":"get_tap","text":"<pre><code>get_tap(namespace, name)\n</code></pre> <p>Hole Metadaten und Versionsliste eines Taps.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>Besitzer.</p> required <code>name</code> <code>str</code> <p>Bundle-Name.</p> required <p>Returns:</p> Type Description <code>tuple[TapEntry, list[TapVersion]]</code> <p>Tuple aus :class:<code>TapEntry</code> und Liste von :class:<code>TapVersion</code>.</p> Source code in <code>src/qualdatan_plugins/server_client.py</code> <pre><code>def get_tap(self, namespace: str, name: str) -&gt; tuple[TapEntry, list[TapVersion]]:\n    \"\"\"Hole Metadaten und Versionsliste eines Taps.\n\n    Args:\n        namespace: Besitzer.\n        name: Bundle-Name.\n\n    Returns:\n        Tuple aus :class:`TapEntry` und Liste von :class:`TapVersion`.\n    \"\"\"\n    payload = self._request(\"GET\", f\"/index/taps/{namespace}/{name}\")\n    if not isinstance(payload, dict) or \"entry\" not in payload:\n        raise PluginServerError(\"get_tap: expected {'entry': ..., 'versions': [...]} payload\")\n    entry = _parse_entry(payload[\"entry\"])\n    versions_raw = payload.get(\"versions\", [])\n    if not isinstance(versions_raw, list):\n        raise PluginServerError(\"get_tap: 'versions' is not a list\")\n    versions = [_parse_version(v) for v in versions_raw]\n    return entry, versions\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerClient.register_tap","title":"register_tap","text":"<pre><code>register_tap(namespace, repo_url)\n</code></pre> <p>Registriere einen neuen Tap (benoetigt Bearer-Token).</p> <p>Der Server verifiziert Besitz des <code>namespace</code> via OAuth und liest <code>bundle.yaml</code> aus <code>repo_url</code> zur Ableitung von <code>name</code> und Metadaten.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>Besitzer (muss zum OAuth-Account passen).</p> required <code>repo_url</code> <code>str</code> <p>Git-URL des Bundle-Repos.</p> required <p>Returns:</p> Type Description <code>TapEntry</code> <p>Der neu registrierte Eintrag.</p> <p>Raises:</p> Type Description <code>PluginServerError</code> <p>Bei fehlendem Token oder Server-Fehler.</p> Source code in <code>src/qualdatan_plugins/server_client.py</code> <pre><code>def register_tap(self, namespace: str, repo_url: str) -&gt; TapEntry:\n    \"\"\"Registriere einen neuen Tap (benoetigt Bearer-Token).\n\n    Der Server verifiziert Besitz des ``namespace`` via OAuth und liest\n    ``bundle.yaml`` aus ``repo_url`` zur Ableitung von ``name`` und\n    Metadaten.\n\n    Args:\n        namespace: Besitzer (muss zum OAuth-Account passen).\n        repo_url: Git-URL des Bundle-Repos.\n\n    Returns:\n        Der neu registrierte Eintrag.\n\n    Raises:\n        PluginServerError: Bei fehlendem Token oder Server-Fehler.\n    \"\"\"\n    if not self._token:\n        raise PluginServerError(\"register_tap requires a bearer token\")\n    payload = self._request(\n        \"POST\",\n        \"/taps\",\n        json_body={\"namespace\": namespace, \"repo_url\": repo_url},\n        auth=True,\n    )\n    if not isinstance(payload, dict) or \"entry\" not in payload:\n        raise PluginServerError(\"register_tap: expected {'entry': ...} payload\")\n    return _parse_entry(payload[\"entry\"])\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerClient.healthz","title":"healthz","text":"<pre><code>healthz()\n</code></pre> <p>Liveness-Check. Gibt das rohe JSON-Dict zurueck.</p> Source code in <code>src/qualdatan_plugins/server_client.py</code> <pre><code>def healthz(self) -&gt; dict:\n    \"\"\"Liveness-Check. Gibt das rohe JSON-Dict zurueck.\"\"\"\n    payload = self._request(\"GET\", \"/healthz\")\n    if not isinstance(payload, dict):\n        raise PluginServerError(\"healthz: expected object payload\")\n    return payload\n</code></pre>"},{"location":"api/#qualdatan_plugins.PluginServerError","title":"PluginServerError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Wird bei Nicht-2xx-Antworten oder malformierten Payloads geworfen.</p>"},{"location":"api/#qualdatan_plugins.TapEntry","title":"TapEntry  <code>dataclass</code>","text":"<pre><code>TapEntry(namespace, name, repo_url, latest_version, label, description, keywords, license)\n</code></pre> <p>Ein Eintrag im Plugin-Server-Index.</p> <p>Attributes:</p> Name Type Description <code>namespace</code> <code>str</code> <p>Besitzer des Taps (z.B. GitHub-Org oder -User).</p> <code>name</code> <code>str</code> <p>Bundle-Name innerhalb des Namespaces.</p> <code>repo_url</code> <code>str</code> <p>Git-URL, unter der das Bundle lebt.</p> <code>latest_version</code> <code>str</code> <p>Zuletzt veroeffentlichte Version (SemVer, ohne <code>v</code>).</p> <code>label</code> <code>str</code> <p>Menschenlesbarer Titel.</p> <code>description</code> <code>str</code> <p>Kurze Beschreibung.</p> <code>keywords</code> <code>tuple[str, ...]</code> <p>Such-Keywords.</p> <code>license</code> <code>str</code> <p>SPDX-Identifier.</p>"},{"location":"api/#qualdatan_plugins.TapEntry.id","title":"id  <code>property</code>","text":"<pre><code>id\n</code></pre> <p>Volle Bundle-Id im Format <code>namespace/name</code>.</p>"},{"location":"api/#qualdatan_plugins.TapVersion","title":"TapVersion  <code>dataclass</code>","text":"<pre><code>TapVersion(version, tag, commit_sha, published_at)\n</code></pre> <p>Eine fuer einen Tap gelistete Version.</p> <p>Attributes:</p> Name Type Description <code>version</code> <code>str</code> <p>SemVer-String, z.B. <code>\"0.3.0\"</code>.</p> <code>tag</code> <code>str</code> <p>Git-Tag, typischerweise <code>\"v\" + version</code>.</p> <code>commit_sha</code> <code>str</code> <p>Commit-SHA des Tags.</p> <code>published_at</code> <code>str</code> <p>ISO-8601-Zeitstempel.</p>"},{"location":"api/#qualdatan_plugins.VerificationReport","title":"VerificationReport  <code>dataclass</code>","text":"<pre><code>VerificationReport(ok, errors, warnings)\n</code></pre> <p>Ergebnis eines Verify-Laufs.</p> <p>Attributes:</p> Name Type Description <code>ok</code> <code>bool</code> <p><code>True</code> wenn keine Fehler aufgetreten sind.</p> <code>errors</code> <code>tuple[str, ...]</code> <p>Liste von Fehler-Meldungen (ok=False wenn nicht leer).</p> <code>warnings</code> <code>tuple[str, ...]</code> <p>Nicht-blockierende Hinweise.</p>"},{"location":"api/#qualdatan_plugins.load_manifest","title":"load_manifest","text":"<pre><code>load_manifest(bundle_root)\n</code></pre> <p>Laedt <code>&lt;bundle_root&gt;/bundle.yaml</code>.</p> <p>Parameters:</p> Name Type Description Default <code>bundle_root</code> <code>Path</code> <p>Verzeichnis, das eine <code>bundle.yaml</code> enthaelt.</p> required <p>Returns:</p> Type Description <code>BundleManifest</code> <p>Das geparste :class:<code>BundleManifest</code>.</p> <p>Raises:</p> Type Description <code>BundleError</code> <p>wenn die Datei fehlt oder das Schema verletzt ist.</p> Source code in <code>src/qualdatan_plugins/bundle.py</code> <pre><code>def load_manifest(bundle_root: Path) -&gt; BundleManifest:\n    \"\"\"Laedt ``&lt;bundle_root&gt;/bundle.yaml``.\n\n    Args:\n        bundle_root: Verzeichnis, das eine ``bundle.yaml`` enthaelt.\n\n    Returns:\n        Das geparste :class:`BundleManifest`.\n\n    Raises:\n        BundleError: wenn die Datei fehlt oder das Schema verletzt ist.\n    \"\"\"\n\n    bundle_root = Path(bundle_root)\n    manifest_path = bundle_root / \"bundle.yaml\"\n    if not manifest_path.is_file():\n        raise BundleError(f\"bundle.yaml nicht gefunden unter {manifest_path}\")\n    with manifest_path.open(\"r\", encoding=\"utf-8\") as f:\n        data = yaml.safe_load(f)\n    return parse_manifest(data or {}, bundle_root)\n</code></pre>"},{"location":"api/#qualdatan_plugins.parse_manifest","title":"parse_manifest","text":"<pre><code>parse_manifest(data, root)\n</code></pre> <p>Baut ein :class:<code>BundleManifest</code> aus einem geparsten Mapping.</p> Source code in <code>src/qualdatan_plugins/bundle.py</code> <pre><code>def parse_manifest(data: Mapping[str, Any], root: Path) -&gt; BundleManifest:\n    \"\"\"Baut ein :class:`BundleManifest` aus einem geparsten Mapping.\"\"\"\n\n    if not isinstance(data, Mapping):\n        raise BundleError(\"bundle.yaml: Top-Level muss ein Mapping sein\")\n\n    bundle_id = _validate_id(data.get(\"id\"))\n    version = data.get(\"version\")\n    if not isinstance(version, str):\n        raise BundleError(\"bundle.yaml: 'version' fehlt oder ist kein String\")\n\n    return BundleManifest(\n        id=bundle_id,\n        version=version,\n        label=str(data.get(\"label\", bundle_id)),\n        description=str(data.get(\"description\", \"\")).strip(),\n        author=str(data.get(\"author\", \"\")),\n        homepage=str(data.get(\"homepage\", \"\")),\n        license=str(data.get(\"license\", \"\")),\n        requires=_parse_requires(data.get(\"requires\")),\n        facets=_as_tuple(data.get(\"facets\"), \"facets\"),\n        codebooks=_as_tuple(data.get(\"codebooks\"), \"codebooks\"),\n        methods=_as_tuple(data.get(\"methods\"), \"methods\"),\n        layouts=_as_tuple(data.get(\"layouts\"), \"layouts\"),\n        root=Path(root).resolve(),\n    )\n</code></pre>"},{"location":"api/#qualdatan_plugins.load_bundle_facets","title":"load_bundle_facets","text":"<pre><code>load_bundle_facets(ib)\n</code></pre> <p>Laedt alle Facets eines installierten Bundles.</p> <p>Jeder Eintrag in <code>manifest.facets</code> darf entweder eine YAML-Datei oder ein Verzeichnis sein; im Verzeichnisfall wird :func:<code>qualdatan_core.facets.load_facets_from_dir</code> genutzt.</p> <p>Parameters:</p> Name Type Description Default <code>ib</code> <code>InstalledBundle</code> <p>Das installierte Bundle.</p> required <p>Returns:</p> Type Description <code>list[Facet]</code> <p>Liste von geladenen :class:<code>Facet</code>-Instanzen (in Manifest-Reihenfolge).</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def load_bundle_facets(ib: InstalledBundle) -&gt; list[Facet]:\n    \"\"\"Laedt alle Facets eines installierten Bundles.\n\n    Jeder Eintrag in ``manifest.facets`` darf entweder eine YAML-Datei oder\n    ein Verzeichnis sein; im Verzeichnisfall wird\n    :func:`qualdatan_core.facets.load_facets_from_dir` genutzt.\n\n    Args:\n        ib: Das installierte Bundle.\n\n    Returns:\n        Liste von geladenen :class:`Facet`-Instanzen (in Manifest-Reihenfolge).\n    \"\"\"\n\n    out: list[Facet] = []\n    for rel in ib.manifest.facets:\n        p = ib.manifest.resolve(rel)\n        if p.is_dir():\n            out.extend(load_facets_from_dir(p))\n        elif p.is_file():\n            out.append(load_facet_from_yaml(p))\n        else:\n            _log.warning(\"Facet-Pfad existiert nicht: %s (%s)\", rel, ib.manifest.ref())\n    return out\n</code></pre>"},{"location":"api/#qualdatan_plugins.load_bundle_layouts","title":"load_bundle_layouts","text":"<pre><code>load_bundle_layouts(ib)\n</code></pre> <p>Laedt alle Layouts eines installierten Bundles aus YAML.</p> <p>Parameters:</p> Name Type Description Default <code>ib</code> <code>InstalledBundle</code> <p>Das installierte Bundle.</p> required <p>Returns:</p> Type Description <code>list[FolderLayout]</code> <p>Liste von :class:<code>FolderLayout</code>-Instanzen (in Manifest-Reihenfolge).</p> Source code in <code>src/qualdatan_plugins/loader.py</code> <pre><code>def load_bundle_layouts(ib: InstalledBundle) -&gt; list[FolderLayout]:\n    \"\"\"Laedt alle Layouts eines installierten Bundles aus YAML.\n\n    Args:\n        ib: Das installierte Bundle.\n\n    Returns:\n        Liste von :class:`FolderLayout`-Instanzen (in Manifest-Reihenfolge).\n    \"\"\"\n\n    out: list[FolderLayout] = []\n    for rel in ib.manifest.layouts:\n        p = ib.manifest.resolve(rel)\n        if not p.is_file():\n            _log.warning(\"Layout-Pfad existiert nicht: %s (%s)\", rel, ib.manifest.ref())\n            continue\n        with p.open(\"r\", encoding=\"utf-8\") as f:\n            data = yaml.safe_load(f) or {}\n        out.append(_build_layout(data, source=str(p)))\n    return out\n</code></pre>"},{"location":"api/#qualdatan_plugins.hash_bundle","title":"hash_bundle","text":"<pre><code>hash_bundle(root)\n</code></pre> <p>Berechnet einen deterministischen SHA-256 ueber alle Bundle-Dateien.</p> <p>Der Hash wird ueber <code>(relpath_bytes + b\"\\0\" + file_bytes + b\"\\0\")</code> pro Datei in sortierter Reihenfolge aufgebaut. <code>.git/</code> und <code>__pycache__/</code> werden ausgeklammert.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path</code> <p>Bundle-Wurzel.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Hex-Digest.</p> <p>Raises:</p> Type Description <code>VerificationError</code> <p>wenn <code>root</code> nicht existiert.</p> Source code in <code>src/qualdatan_plugins/verify.py</code> <pre><code>def hash_bundle(root: Path) -&gt; str:\n    \"\"\"Berechnet einen deterministischen SHA-256 ueber alle Bundle-Dateien.\n\n    Der Hash wird ueber ``(relpath_bytes + b\"\\\\0\" + file_bytes + b\"\\\\0\")`` pro\n    Datei in sortierter Reihenfolge aufgebaut. ``.git/`` und ``__pycache__/``\n    werden ausgeklammert.\n\n    Args:\n        root: Bundle-Wurzel.\n\n    Returns:\n        Hex-Digest.\n\n    Raises:\n        VerificationError: wenn ``root`` nicht existiert.\n    \"\"\"\n\n    root = Path(root)\n    if not root.exists() or not root.is_dir():\n        raise VerificationError(f\"Bundle-root existiert nicht: {root}\")\n\n    h = hashlib.sha256()\n    for f in _iter_bundle_files(root):\n        rel = f.relative_to(root).as_posix().encode(\"utf-8\")\n        h.update(rel)\n        h.update(b\"\\0\")\n        h.update(f.read_bytes())\n        h.update(b\"\\0\")\n    return h.hexdigest()\n</code></pre>"},{"location":"api/#qualdatan_plugins.verify_installed","title":"verify_installed","text":"<pre><code>verify_installed(ib)\n</code></pre> <p>Wie :func:<code>verify_manifest</code>, plus Pfad-Checks am Install-Verzeichnis.</p> <p>Parameters:</p> Name Type Description Default <code>ib</code> <code>InstalledBundle</code> <p>Das installierte Bundle.</p> required <p>Returns:</p> Type Description <code>VerificationReport</code> <p>Ein kombiniertes :class:<code>VerificationReport</code>.</p> Source code in <code>src/qualdatan_plugins/verify.py</code> <pre><code>def verify_installed(ib: InstalledBundle) -&gt; VerificationReport:\n    \"\"\"Wie :func:`verify_manifest`, plus Pfad-Checks am Install-Verzeichnis.\n\n    Args:\n        ib: Das installierte Bundle.\n\n    Returns:\n        Ein kombiniertes :class:`VerificationReport`.\n    \"\"\"\n\n    base = verify_manifest(ib.manifest)\n    errors = list(base.errors)\n    warnings = list(base.warnings)\n\n    if not ib.install_path.exists():\n        errors.append(f\"install_path existiert nicht: {ib.install_path}\")\n    else:\n        try:\n            same = ib.install_path.resolve() == ib.manifest.root.resolve()\n        except OSError as e:\n            same = False\n            errors.append(f\"install_path nicht aufloesbar: {e}\")\n        if not same:\n            errors.append(\n                f\"install_path != manifest.root ({ib.install_path} vs {ib.manifest.root})\"\n            )\n\n    return VerificationReport(ok=not errors, errors=tuple(errors), warnings=tuple(warnings))\n</code></pre>"},{"location":"api/#qualdatan_plugins.verify_manifest","title":"verify_manifest","text":"<pre><code>verify_manifest(manifest)\n</code></pre> <p>Prueft das Manifest gegen das Dateisystem.</p> <p>Parameters:</p> Name Type Description Default <code>manifest</code> <code>BundleManifest</code> <p>Geparstes Manifest (typischerweise aus :func:<code>qualdatan_plugins.bundle.load_manifest</code>).</p> required <p>Returns:</p> Name Type Description <code>Ein</code> <code>VerificationReport</code> <p>class:<code>VerificationReport</code> mit Fehlern/Warnungen.</p> Source code in <code>src/qualdatan_plugins/verify.py</code> <pre><code>def verify_manifest(manifest: BundleManifest) -&gt; VerificationReport:\n    \"\"\"Prueft das Manifest gegen das Dateisystem.\n\n    Args:\n        manifest: Geparstes Manifest (typischerweise aus\n            :func:`qualdatan_plugins.bundle.load_manifest`).\n\n    Returns:\n        Ein :class:`VerificationReport` mit Fehlern/Warnungen.\n    \"\"\"\n\n    errors: list[str] = []\n    warnings: list[str] = []\n\n    # id / version format double-check (bundle.py enforces id at parse time,\n    # aber hier haerten wir auch version ab).\n    if not _BUNDLE_ID_RE.match(manifest.id):\n        errors.append(f\"ungueltige bundle-id: {manifest.id!r}\")\n    if not _SEMVER_RE.match(manifest.version):\n        errors.append(f\"ungueltige version: {manifest.version!r}\")\n\n    # referenzierte Pfade\n    _check_paths(manifest.root, manifest.facets, \"facets\", errors)\n    _check_paths(manifest.root, manifest.codebooks, \"codebooks\", errors)\n    _check_paths(manifest.root, manifest.methods, \"methods\", errors)\n    _check_paths(manifest.root, manifest.layouts, \"layouts\", errors)\n\n    # requires\n    core_req = [r for r in manifest.requires if r.package == \"qualdatan-core\"]\n    if not core_req:\n        errors.append(\"requires.qualdatan-core fehlt\")\n    else:\n        spec = core_req[0].version_spec\n        if not spec or not isinstance(spec, str):\n            errors.append(f\"requires.qualdatan-core version_spec leer: {spec!r}\")\n\n    # license\n    if not manifest.license.strip():\n        warnings.append(\"license ist leer\")\n\n    return VerificationReport(ok=not errors, errors=tuple(errors), warnings=tuple(warnings))\n</code></pre>"},{"location":"architecture/","title":"Architecture","text":"<p><code>qualdatan-plugins</code> ist ein Tap-Style-Plugin-Manager: Bundles (YAML-Daten-Pakete) liegen in GitHub-Repos der Publisher. Der Plugin-Manager selbst kennt keine Dom\u00e4ne, sondern nur das Bundle-Format.</p>"},{"location":"architecture/#rolle-der-subpakete-phase-c","title":"Rolle der Subpakete (Phase C)","text":"Modul Verantwortlich f\u00fcr <code>manager</code> High-level Einstieg: install, update, remove <code>registry</code> Lokaler Index installierter Bundles <code>loader</code> YAML \u2192 Facet-Instanzen (via <code>qualdatan_core.facets.loader</code>) <code>verify</code> Signatur-/Hash-Pr\u00fcfung <code>cache</code> Download-Cache (via <code>platformdirs</code>) <code>server_client</code> HTTP-Client zum Plugin-Server (Listing/Suche) <code>bundle</code> Bundle-Schema und Serialisierung"},{"location":"architecture/#abgrenzung","title":"Abgrenzung","text":"<ul> <li>Facet-Typen (Klassen) kommen aus <code>qualdatan-core</code> bzw. aus Entry-Point-Plugins \u2014 nicht aus diesem Repo.</li> <li>Bundles liefern nur Konfigurationen/Daten, keine Python-Klassen.</li> <li>Plugin-Server ist ein separates Projekt und liefert nur Listings.</li> </ul>"},{"location":"architecture/#lizenz-spdx","title":"Lizenz &amp; SPDX","text":"<p>AGPL-3.0-only. Neue Quelldateien beginnen mit:</p> <pre><code># SPDX-License-Identifier: AGPL-3.0-only\n</code></pre>"},{"location":"changelog/","title":"Changelog","text":"<p>Alle relevanten \u00c4nderungen werden hier dokumentiert. Format: Keep a Changelog. Versionierung: SemVer.</p>"},{"location":"changelog/#unreleased","title":"[Unreleased]","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li>Docs-Setup (MkDocs Material + mkdocstrings), GitHub-Pages-Deployment via CI.</li> </ul>"}]}